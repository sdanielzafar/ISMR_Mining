README FOR VISUALIZATION SCRIPTS

1) Make sure to change the reference directories for whever you are storing your files:

2) make sure to use:

install.packages(maps) 

for the cluster visualization script and for any other libraries that 'don't exist'

3) Note that the EOF script also uses the NetCDF import script to ascertain where values are NA. Either comment out or just 
import the corresponding netCDF file to run it. 